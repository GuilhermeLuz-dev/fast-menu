import { StyleSheet } from "react-native";

export const colors = {
  light: "#fff",
  dark: "#000",
  secondary: "#3C2F2F",
};

export const global = StyleSheet.create({
  container: {
    width: "100%",
    paddingHorizontal: 20,
  },
});
